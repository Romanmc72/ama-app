import { StyleSheet } from 'react-native';

export const viewStyles = StyleSheet.create({
  view: {
    alignItems: 'center',
    justifyContent: 'center',
    minHeight: '100%',
    width: '100%',
  },
});
